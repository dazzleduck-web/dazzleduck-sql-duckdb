# name: test/sql/dazzle_duck.test
# description: test dazzle_duck extension
# group: [sql]

# Before we load the extension, this will fail
statement error
SELECT dazzle_duck_version();
----
Catalog Error: Scalar Function with name dazzle_duck_version does not exist!

# Require statement will ensure this test is run with this extension loaded
require dazzle_duck

# Confirm the extension works
query I
SELECT length(dazzle_duck_version()) > 0;
----
true
